{
  "name": "angular-nestjs-fullstack-starter",
  "private": true,
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "check:git": "bash scripts/check-repo.sh",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
